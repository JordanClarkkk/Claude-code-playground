# Anthropic API key for Claude-powered PDF extraction.
# Copy this file to .env and fill in your key:
#   cp .env.example .env
#
# When set, users won't need to enter a key in the browser.
# The key is never exposed to the frontend.
ANTHROPIC_API_KEY=sk-ant-...
